[package]
name = "scalattice-agent"
version = "1.1.44"
edition = "2021"
description = "Open-source GPU machine agent for the Scalattice inference network"
license = "MIT"
repository = "https://github.com/scalattice/scalattice-agent"
readme = "README.md"
keywords = ["scalattice", "gpu", "inference", "websocket"]
categories = ["command-line-utilities", "network-programming"]

[[bin]]
name = "scalattice-agent"
path = "src/main.rs"

[build-dependencies]
winres = "0.1"

[dependencies]
anyhow = "1"
base64 = "0.22"
clap = { version = "4", features = ["derive", "env"] }
encoding_rs = "0.8"
futures-util = { version = "0.3", default-features = false, features = ["std", "io"] }
llama-cpp-2 = { version = "0.1.154", default-features = false, features = ["openmp", "sampler", "mtmd"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "sync", "time", "fs", "io-util", "process"] }
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream", "json"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
url = "2"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
flate2 = "1"
tar = "0.4"

[target.'cfg(windows)'.dependencies]
# No default features: omit Linux gtk/libappindicator from the lockfile (glib advisory).
tray-icon = { version = "0.24", default-features = false }
eframe = { version = "0.31", default-features = false, features = ["default_fonts", "glow"] }
image = "0.25"
tauri-winrt-notification = "0.8"
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_Security", "Win32_Security_Authorization", "Win32_Storage_FileSystem", "Win32_System_Console", "Win32_System_Diagnostics_ToolHelp", "Win32_System_Threading", "Win32_UI_Shell", "Win32_UI_WindowsAndMessaging"] }

[target.'cfg(target_os = "macos")'.dependencies]
tray-icon = { version = "0.24", default-features = false }
eframe = { version = "0.31", default-features = false, features = ["default_fonts", "glow"] }
image = "0.25"

[features]
default = ["gpu"]
# x86_64 Linux: NVIDIA CUDA + AMD/Intel Vulkan; release bundles non-driver .so deps
gpu = ["cuda", "vulkan"]
# Windows release: CUDA + Vulkan (AMD/Intel). Needs Vulkan SDK on the build host (glslc).
win-gpu = ["cuda", "vulkan"]
# aarch64 native: NVIDIA CUDA + Vulkan (Jetson, Graviton, Mali, Adreno)
arm-gpu = ["cuda", "vulkan"]
cuda = ["llama-cpp-2/cuda"]
vulkan = ["llama-cpp-2/vulkan"]
metal = ["llama-cpp-2/metal"]
dynamic-backends = ["llama-cpp-2/dynamic-backends"]
rocm = ["llama-cpp-2/rocm"]

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
